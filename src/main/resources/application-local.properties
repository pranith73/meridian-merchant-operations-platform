# ------------------------------------------------------------
# Local profile
# ------------------------------------------------------------
# Use this profile for local development on your own machine.
# Keep it safe for personal development and debugging.
# Do not put real secrets in this file.
# ------------------------------------------------------------

# Make local debugging easier.
logging.level.com.pranith73.meridian=DEBUG

# Example rule for future DB config:
# local secrets must come from environment variables or external config,
# not from committed source files.
#
# Example only (do not enable until datasource is introduced):
# spring.datasource.url=${MERIDIAN_DB_URL}
# spring.datasource.username=${MERIDIAN_DB_USERNAME}
# spring.datasource.password=${MERIDIAN_DB_PASSWORD}