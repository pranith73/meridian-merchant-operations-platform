# ------------------------------------------------------------
# Dev profile
# ------------------------------------------------------------
# Use this profile for shared development environments.
# Keep behavior stable and less noisy than local.
# Secrets must still remain externalized.
# ------------------------------------------------------------

logging.level.com.pranith73.meridian=INFO

# Example rule for future DB config:
# spring.datasource.url=${MERIDIAN_DB_URL}
# spring.datasource.username=${MERIDIAN_DB_USERNAME}
# spring.datasource.password=${MERIDIAN_DB_PASSWORD}